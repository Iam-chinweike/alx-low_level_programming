#include <stdio.h>
/**
 * main-prints the alphabets in a reverse other
 * Return: 0 on success
 */
int main(void)
{
char foo;
for (foo = 'z'; foo >= ; foo--)
{
putchar(foo);
}
putchar('\n');
return (0);
}

#include <stdio.h>
/**
 * main - prints  numbers of base ten with a loop function
 * Return: 0 on success
 */
int main(void)
{
for (i = 0; i < 10; i++)
{
putchar('0' + i);
}
putchar('\n');
return (0);
}

#include "main.h"
/**
 *_abs - prints the absolute value
 *@d: is the type of the argument
 *Return: the absolute value
 */
int _abs(int d)
{
int abval;
if (d < 0)
{
abval = d * -1;
}
else if (d >= 0)
{
abval = d;
}
return (d);
}

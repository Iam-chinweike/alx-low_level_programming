#include "main.h"
/**
 *_abs - prints the absolute value
 *@int: is the type of the argument
 *Return: the absolute value
 */
int _abs(int)
{
if (d < 0)
{
int abval;
abval = d * -1;
return (abval);
}
return (d);
}

#include "main.h"
/**
 *_abs - prints the absolute value
 *@d: is the type of the argument
 *Return: the absolute value
 */
int _abs(int d)
{
if (d < 0)
{
return (d * -1);
}
else if (d >= 0)
{
return (d);
}
else 
{
return (d);
}
}
